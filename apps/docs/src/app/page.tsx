import React from "react";
import Homepage from "../components/Homepage/Homepage";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Homepage />
    </div>
  );
};

export default page;
